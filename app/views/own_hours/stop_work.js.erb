$('#own_hour_duration').val('<%= number_to_duration @time_working %>')

if(!$('.own_hour_form').is(':visible')){
  $('a.add_own_hours_time_button').trigger('click');
}

$('.add_own_hours_button').removeClass("none");
$('.stopwatch span').html('');
$(".stop_work_action").hide();

stop_own_hours_stopwatch();

$('.stopwatch').hide();

