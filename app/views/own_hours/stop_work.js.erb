/*
   <%= render :partial => 'cancel_work' %>
 */
$('#own_hour_duration').val('<%= number_to_duration parent.time_working %>')

if(!$('.own_hour_form').is(':visible')){
  $('a:contains("Add Own Hours")').trigger('click');
}

